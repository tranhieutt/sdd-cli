---
# ═══════════════════════════════════════════════════════════════
# SKILL TEMPLATE — SDD Standard v1.0
# Copy file này, đổi tên thư mục, điền các fields bên dưới.
# Fields đánh dấu [REQUIRED] phải có. [OPTIONAL] có thể bỏ qua.
# ═══════════════════════════════════════════════════════════════

name: your-skill-name               # [REQUIRED] kebab-case, khớp tên thư mục
type: workflow                       # [REQUIRED] "workflow" | "reference"
                                     #   workflow  = có bước thực thi, nhận arguments, agent chạy theo flow
                                     #   reference = thư viện pattern/knowledge, không có argument, chỉ đọc
description: "Khi nào invoke skill này — viết dạng search query, không phải label.
  Ví dụ tốt: 'Khi cần review security trước khi deploy production'
  Ví dụ xấu:  'Security review skill'"
                                     # [REQUIRED]
argument-hint: "[no arguments]"      # [REQUIRED nếu type=workflow] Gợi ý arg, ví dụ: "[path]", "[new|update|status]"
                                     # [BỎ QUA nếu type=reference]
user-invocable: true                 # [REQUIRED] true nếu user gọi trực tiếp; false nếu chỉ agent dùng
allowed-tools: Read, Glob, Grep      # [REQUIRED] Liệt kê tường minh — không dùng "*"
effort: 2                            # [REQUIRED] 1=nhanh (<2 phút), 3=trung bình, 5=phức tạp (>15 phút)
agent: lead-programmer               # [OPTIONAL] Agent persona xử lý skill này
when_to_use: "..."                   # [OPTIONAL] Bổ sung context khi nào dùng (tiếng Việt ok)
context: fork                        # [OPTIONAL] "fork" | "!bash-command" | bỏ qua
---

# [Skill Title]

## Mục đích

[Một đoạn ngắn mô tả skill này làm gì và khi nào nên dùng.]

---

## Workflow

### 1. [Bước đầu tiên]

[Mô tả bước này làm gì]

### 2. [Bước tiếp theo]

[...]

---

## Output Format

[Mô tả format output mong đợi — bảng, markdown, checklist...]

---

## Edge Cases

- **[Trường hợp đặc biệt]**: [Cách xử lý]

---

## Related Skills

- `[skill-name]` — [mô tả ngắn khi nào dùng thay thế]
